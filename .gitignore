node_modules/
web/dist/

# Runtime state: pids, ports and logs for the running instance
.run/

# Local database and API cache
data/db.json
data/cache.json

# Local backups (make backup / hermit backup)
backups/
hermit-backup-*.tar.gz

.DS_Store
*.log

# npm pack / install artifacts
*.tgz
